# 仅 Spring 配置。不读取 .env 文件。
#
# 推荐：密钥写在 application-local.yml（gitignore），然后：
#   mvn spring-boot:run -Dspring-boot.run.profiles=local
#
# 属性不要写反：
#   api-key  = 密钥（sk-... / tp-...）
#   base-url = https://... 主机根路径，不要带 /v1

spring:
  main:
    web-application-type: none
    banner-mode: off
  ai:
    openai:
      # 可用环境变量覆盖：SPRING_AI_OPENAI_API_KEY 等
      # 注意：${VAR:} 空默认会变成 ""，Spring AI 会直接启动失败；base-url/model 必须有非空默认
      api-key: ${SPRING_AI_OPENAI_API_KEY:}
      base-url: ${SPRING_AI_OPENAI_BASE_URL:https://api.openai.com}
      chat:
        options:
          model: ${SPRING_AI_OPENAI_CHAT_OPTIONS_MODEL:gpt-4o-mini}
          temperature: 0.0

app:
  # 文件/ls/grep/edit 沙箱根；run_command 的 cwd 也用此路径
  workspace-root: ${user.dir}
  verbose: true
  http-log: true
  http-log-dir: logs
  verbose-char-limit: 2000
  # run_command 超时（秒）；至少 1
  shell-timeout-seconds: 30
  # 危险命令子串拦截（大小写不敏感，空白压缩后匹配）。
  # 若列表为空，代码回退到 CommandGuard.DEFAULT_BLOCKED_PATTERNS。
  shell-blocked-patterns:
    - "rm -rf /"
    - "rm -rf/*"
    - "rm -rf ~"
    - "mkfs"
    - "dd if="
    - ":(){"
    - "shutdown"
    - "reboot"
    - "halt"
    - "poweroff"
    - "curl|sh"
    - "curl | sh"
    - "wget|sh"
    - "wget | sh"
    - "sudo "
    - "chmod -r 777 /"
    - "> /dev/sd"

logging:
  level:
    root: INFO
    cli.trace: INFO
    com.example.simplecliagent: INFO
    org.springframework.ai: INFO
